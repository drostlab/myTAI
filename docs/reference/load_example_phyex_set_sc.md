# Load Example Single-Cell PhyloExpressionSet

Creates and returns an example ScPhyloExpressionSet object for testing
and examples. Only works if Seurat is installed.

## Usage

``` r
load_example_phyex_set_sc()
```

## Value

A ScPhyloExpressionSet object, or NULL if Seurat is not available.
