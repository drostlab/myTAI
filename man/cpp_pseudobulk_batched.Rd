% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{cpp_pseudobulk_batched}
\alias{cpp_pseudobulk_batched}
\title{Create Pseudobulk Expression Matrix (Memory-Optimized Implementation)}
\usage{
cpp_pseudobulk_batched(
  expression,
  groups,
  n_groups,
  batch_size = 1000L,
  ncores = 10L
)
}
\arguments{
\item{expression}{Sparse expression matrix (genes x cells) - dgCMatrix format}

\item{groups}{Integer vector indicating group membership for each cell (0-based indexing)}

\item{n_groups}{Integer, total number of unique groups}

\item{batch_size}{Integer, number of genes to process per batch (default: 1000)}

\item{ncores}{Integer, number of cores to use for parallel processing (default: 10)}
}
\value{
Dense matrix of pseudobulked expression (genes x groups)
}
\description{
Memory-optimized version that processes the pseudobulking in batches
for very large datasets to avoid memory issues.
}
\details{
This function processes genes in batches to manage memory usage for very large
single-cell datasets. It's particularly useful when dealing with hundreds of
thousands of cells and tens of thousands of genes.
}
\keyword{internal}
